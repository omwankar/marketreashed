# Radar page — Gemini is tried first; NVIDIA is fallback if Gemini fails
VITE_GEMINI_API_KEY=your-gemini-key-here

# NVIDIA Integrate API (build.nvidia.com) — used via /api/nvidia proxy when Gemini fails
VITE_NVIDIA_API_KEY=nvapi-your-key-here

# Contact form (EmailJS)
VITE_EMAILJS_PUBLIC_KEY=
